# Bağımlılıklar
node_modules/

# Gizli anahtarlar — ASLA git'e gitmez
.env
backend/.env

# Önbellek ve loglar
backend/cache/
*.log

# Kullanıcı kupon verisi (hesaba bağlı, kalıcı depo)
# + EV motoru gölge kayıtları (backend/data/ev-shadow/) — iç ölçüm verisi
backend/data/

# Derleme çıktıları
# backend/public/ ARTIK DEPOYA GİRER (2026-08-11): Flutter web derlemesi
# burada durur ve kök adresi (onrender) o sunar. Render'ın node ortamında
# Flutter SDK olmadığı için çıktı dağıtım anında üretilemez; yerelde
# `flutter-web-yayinla.ps1` ile derlenip işlenir.
app/dist/
app/.expo/
dist/

# Sistem
.DS_Store
Thumbs.db

# Geçici/denetim klasörleri ve sır yedekleri — ASLA git'e gitmez
_to_delete/
_sync/
_audit_fresh/
app/.audit-web-*/
*env-yedek*
# .env'in HER türevi (.env.yedek-0107, .env.bak, .env.local ...) — örnek dosya hariç
.env.*
!.env.example
!backend/.env.example
*.zip
*.tar.gz
