# Bağımlılıklar
node_modules/

# Gizli anahtarlar — ASLA git'e gitmez
.env
backend/.env

# Önbellek ve loglar
backend/cache/
*.log

# Kullanıcı kupon verisi (hesaba bağlı, kalıcı depo)
# + EV motoru gölge kayıtları (backend/data/ev-shadow/) — iç ölçüm verisi
backend/data/

# Derleme çıktıları
backend/public/
app/dist/
app/.expo/
dist/

# Sistem
.DS_Store
Thumbs.db

# Geçici/denetim klasörleri ve sır yedekleri — ASLA git'e gitmez
_to_delete/
_sync/
_audit_fresh/
app/.audit-web-*/
*env-yedek*
*.zip
*.tar.gz
