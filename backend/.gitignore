# Backend üretilen/yerel veriler — repoya ve deploy paketine GİRMEZ.
node_modules/
cache/
data/
public/
.env

# LEGACY TEKNİK YEDEK — eski radar/snapshot kayıtları (npm run archive:legacy).
# Production build'e, Render deploy'una, runtime container'a ve bundle'a girmez;
# API'den statik servis edilmez. Manifest ile birlikte yalnız yerel yedektir.
legacy_archive/
